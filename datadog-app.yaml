project: default
source:
  repoURL: 'https://github.com/ahan0123/argocd-datadog.git'
  path: .
  helm:
    valueFiles:
      - values-dockerd.yaml
destination:
  server: 'https://kubernetes.default.svc'
  namespace: default
